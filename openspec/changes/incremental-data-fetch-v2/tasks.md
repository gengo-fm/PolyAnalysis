## 1. 缓存模块重构

- [x] 1.1 创建 `polycopilot/cache_v2/` 目录结构
- [x] 1.2 实现 `MemoryCache` 类（内存缓存，支持 TTL）
- [x] 1.3 实现 `DiskCache` 类（文件缓存，支持原子写入）
- [x] 1.4 实现 `CacheManager` 类（统一接口，协调三层缓存）
- [x] 1.5 添加缓存验证（JSON 损坏检测）

## 2. 增量获取模块

- [ ] 2.1 修改 `fetcher.py` 使用缓存模块
- [ ] 2.2 实现时间戳增量逻辑
- [ ] 2.3 实现数据合并逻辑
- [ ] 2.4 添加 API 限流处理
- [ ] 2.5 测试增量 vs 全量获取

## 3. 进度跟踪模块

- [x] 3.1 创建 `_progress.json` 数据结构（已在 CacheManager 中实现）
- [x] 3.2 实现 `save_progress()` 函数
- [x] 3.3 实现 `load_progress()` 函数
- [x] 3.4 实现断点续传逻辑
- [x] 3.5 实现失败重试队列（3 次重试后跳过）

## 4. 批量处理模块

- [x] 4.1 实现 `BatchProcessor` 类
- [x] 4.2 配置并发数（20）和限流（50/分钟）
- [x] 4.3 添加批量统计报告
- [x] 4.4 添加进度显示（百分比 + ETA）

## 5. CLI 集成

- [ ] 5.1 修改 `analyze.py` 支持 `--batch` 模式
- [ ] 5.2 添加 `--resume` 参数支持断点续传
- [ ] 5.3 添加 `--force-refresh` 强制刷新缓存
- [ ] 5.4 添加 `--parallel` 参数控制并发数

## 6. 测试与验证

- [ ] 6.1 测试三层缓存流程
- [ ] 6.2 测试增量更新（模拟新数据）
- [ ] 6.3 测试断点续传（中断后恢复）
- [ ] 6.4 测试批量处理性能
- [ ] 6.5 验证生成报告的正确性

## 7. 文档更新

- [ ] 7.1 更新 SKILL.md 说明新增功能
- [ ] 7.2 更新 README.md 使用说明
- [ ] 7.3 添加 OpenSpec 到版本控制
